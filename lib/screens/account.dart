import 'package:flutter/material.dart';
class AccountScreen extends StatelessWidget { const AccountScreen({super.key}); @override Widget build(BuildContext context)=>Scaffold(appBar:AppBar(title:const Text('My Account')),body:ListView(padding:const EdgeInsets.all(16),children:[const CircleAvatar(radius:36,child:Icon(Icons.person,size:40)),const SizedBox(height:12),const Center(child:Text('Guest User',style:TextStyle(fontSize:20,fontWeight:FontWeight.bold))),const SizedBox(height:20),for(final x in ['Orders','Licenses','Downloads','Wishlist','Notifications','Payment History','Settings'])ListTile(leading:const Icon(Icons.chevron_right),title:Text(x),onTap:(){}),const SizedBox(height:10),FilledButton.icon(onPressed:(){},icon:const Icon(Icons.login),label:const Text('Login / Register'))]));}
